<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Items</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
    />
  </head>
  <body
    class="bg-gradient-to-r from-blue-500 via-purple-600 to-indigo-700 min-h-screen flex flex-col justify-center items-center"
  >
    <div class="container mx-auto p-4 bg-white rounded-lg shadow-lg">
      <h2 class="text-2xl font-bold mb-4 text-center text-gray-800">
        Item info
      </h2>

      <div
        class="grid grid-cols-1 md:grid-cols-1 lg:grid-cols-1 gap-4 justify-center"
      >
        <% if(result.length > 0) { %> <% result.forEach((eachData) => { %>
        <div
          class="bg-gray-100 rounded-lg shadow-md p-6 justify-center hover:shadow-lg transition duration-300 ease-in-out"
        >
          <h1 class="text-3xl font-semibold mb-2 text-gray-800">
            <%- eachData.itemName %>
          </h1>
          <p class="text-lg text-gray-700">
            <strong>Item Code:</strong> <%- eachData.item_code %><br />
            <strong>Item Name:</strong> <%- eachData.item_name %><br />
            <strong>Barcode:</strong> <%- eachData.barcode %><br />
            <strong>HSN Code:</strong> <%- eachData.hsncode %><br />
            <strong>Department:</strong> <%- eachData.deptname %><br />
            <strong>Sub Department:</strong> <%- eachData.subdeptname %><br />
            <strong>HSN Group Code:</strong> <%- eachData.hsngcode %><br />
          </p>
          <div class="mt-4">
            <a
              href="/update-data/?item_code=<%- eachData.item_code %>"
              class="btn btn-sm btn-outline-primary me-1 hover:bg-blue-600 hover:text-white transition duration-300 ease-in-out"
              >Update</a
            >
            <a
              href="/delete_item/?id=<%- eachData.id %>"
              class="btn btn-sm btn-outline-danger hover:bg-red-600 hover:text-white transition duration-300 ease-in-out"
              >Delete</a
            >
          </div>
        </div>
        <% }); %> <% } else { %>
        <div class="text-center">
          <p class="text-lg text-gray-800">No data found</p>
        </div>
        <% } %>
      </div>
      <a href="/" class="btn btn-primary mt-4">Add Item</a>
    </div>
  </body>
</html>
